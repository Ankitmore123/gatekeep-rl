services:
  redis:
    image: redis:7.4-alpine
    container_name: rate_limiter_redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
  app:
    build: .
    container_name: rate_limiter_app
    ports:
      - "8000:8000"
    depends_on:
      - redis

volumes:
  redis_data: